export const addition = (a, b) => {
    return a+b;
}

export const subtraction = (a, b) => {
    return a-b;
}

// only one default function available in the file
export default function multiply(a,b) {
    return a*b;
}

export const divied = (a, b) => {
    return a/b;
}